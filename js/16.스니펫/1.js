// 1. log + tap
console.log();

// 2. fu(function statement) + tap
function name(params) {
	
}

/**
 * 파라미터를 콘솔에 출력한다.
 * @param {*} params 
 */
function name(params) {
	console.log(params);
}

// 3. for(For Loof) + tap
for (let i = 0; i < array.length; i++) {
	const element = array[i];
	
}

// 4. forea, foreach Loof
array.forEach(element => {
	
});

// 5. forin
for (const key in object) {
	if (Object.hasOwnProperty.call(object, key)) {
		const element = object[key];
		
	}
}

// 6. if, if statement 
if (condition) {
	
}

// 7. ifelse, ifelse statement

if (condition) {
	
} else {
	
}
